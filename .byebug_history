exit
current.fetch("time")
current.keys
currently.keys
currently.fetch("time")
currently.fetch("data")
hourly.fetch("data").at(0)
hourly.fetch("data")
hourly.fetch("icon")
hourly.fetch("summary")
hourly.keys
current.fetch("summary")
current.fetch("temperature").class
current.fetch("temperature")
current.keys
hourly.fetch("data").length
hourly.fetch("data")
hourly.keys
exit
hourly
